import CauseSlider from "@/components/causes/causes-slider";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Causes Slider HopeHUB",
};
const index = () => {
   return (
      <Wrapper>
         <CauseSlider />
      </Wrapper>
   )
}

export default index